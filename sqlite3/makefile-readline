# Makefile

CC = gcc
CCFLAGS = -Wall -std=c99 -O2

sqlite3: sqlite3.c
	$(CC) $(CCFLAGS) -DHAVE_READLINE shell.c sqlite3.c -lpthread -ldl -lreadline -o sqlite3
	#strip sqlite3

clean:
	rm -f sqlite3 sqlite3.o

install:
	cp -f sqlite3 /usr/local/bin

uninstall:
	rm -f /usr/local/bin/sqlite3
